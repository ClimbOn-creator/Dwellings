{
  "name": "dwelling-iq-cloudflare",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "flutter run -d chrome",
    "build": "node tool/verify_prebuilt.mjs",
    "build:flutter": "bash tool/build_web.sh",
    "deploy": "npm run build:flutter && wrangler pages deploy dist",
    "cf:types": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
